<div class="floating-bg pointer-events-none absolute inset-0 overflow-hidden" aria-hidden="true">
  <!-- Stars -->
  <span class="floating-star star-1">✦</span>
  <span class="floating-star star-2">★</span>
  <span class="floating-star star-3">✦</span>
  <span class="floating-star star-4">★</span>
  <span class="floating-star star-5">✦</span>
  <span class="floating-star star-6">★</span>
  <span class="floating-star star-7">✦</span>
  <span class="floating-star star-8">★</span>

  <!-- Balloons -->
  <div class="floating-balloon balloon-1" style="--balloon-color: oklch(0.72 0.18 250);">
    <div class="balloon-body"></div>
    <div class="balloon-knot"></div>
    <div class="balloon-string"></div>
  </div>
  <div class="floating-balloon balloon-2" style="--balloon-color: oklch(0.78 0.16 230);">
    <div class="balloon-body"></div>
    <div class="balloon-knot"></div>
    <div class="balloon-string"></div>
  </div>
  <div class="floating-balloon balloon-3" style="--balloon-color: oklch(0.7 0.2 280);">
    <div class="balloon-body"></div>
    <div class="balloon-knot"></div>
    <div class="balloon-string"></div>
  </div>
  <div class="floating-balloon balloon-4" style="--balloon-color: oklch(0.75 0.15 210);">
    <div class="balloon-body"></div>
    <div class="balloon-knot"></div>
    <div class="balloon-string"></div>
  </div>
  <div class="floating-balloon balloon-5" style="--balloon-color: oklch(0.68 0.17 265);">
    <div class="balloon-body"></div>
    <div class="balloon-knot"></div>
    <div class="balloon-string"></div>
  </div>
  <div class="floating-balloon balloon-6" style="--balloon-color: oklch(0.8 0.14 240);">
    <div class="balloon-body"></div>
    <div class="balloon-knot"></div>
    <div class="balloon-string"></div>
  </div>
</div>

<style>
  .floating-bg {
    z-index: 0;
  }

  .floating-star {
    position: absolute;
    color: oklch(0.75 0.14 255 / 55%);
    animation: star-float 6s ease-in-out infinite, star-twinkle 3s ease-in-out infinite;
    line-height: 1;
    user-select: none;
  }

  :global(.dark) .floating-star {
    color: oklch(0.85 0.12 240 / 65%);
  }

  .star-1 { top: 8%; left: 6%; font-size: 1.1rem; animation-delay: 0s, 0.2s; }
  .star-2 { top: 14%; right: 10%; font-size: 0.85rem; animation-delay: -1.2s, 0.8s; }
  .star-3 { top: 32%; left: 4%; font-size: 0.7rem; animation-delay: -2.4s, 1.4s; }
  .star-4 { top: 48%; right: 6%; font-size: 1rem; animation-delay: -0.8s, 0.5s; }
  .star-5 { bottom: 28%; left: 10%; font-size: 0.9rem; animation-delay: -3s, 1.1s; }
  .star-6 { bottom: 18%; right: 14%; font-size: 1.15rem; animation-delay: -1.8s, 1.7s; }
  .star-7 { top: 62%; left: 16%; font-size: 0.75rem; animation-delay: -2.8s, 2.2s; }
  .star-8 { top: 22%; left: 42%; font-size: 0.65rem; animation-delay: -4s, 0.3s; }

  .floating-balloon {
    position: absolute;
    display: flex;
    flex-direction: column;
    align-items: center;
    opacity: 0.75;
    animation: balloon-float 8s ease-in-out infinite;
  }

  :global(.dark) .floating-balloon {
    opacity: 0.85;
  }

  .balloon-body {
    width: var(--balloon-w, 2.5rem);
    height: calc(var(--balloon-w, 2.5rem) * 1.22);
    border-radius: 50% 50% 50% 50% / 55% 55% 45% 45%;
    background: linear-gradient(
      145deg,
      color-mix(in oklch, var(--balloon-color) 90%, white 10%),
      var(--balloon-color)
    );
    box-shadow: inset -4px -6px 12px oklch(0 0 0 / 12%);
  }

  .balloon-knot {
    width: 0.35rem;
    height: 0.35rem;
    margin-top: -0.1rem;
    background: var(--balloon-color);
    transform: rotate(45deg);
  }

  .balloon-string {
    width: 1px;
    height: 2.5rem;
    background: linear-gradient(to bottom, oklch(0.5 0.05 255 / 50%), transparent);
  }

  :global(.dark) .balloon-string {
    background: linear-gradient(to bottom, oklch(0.75 0.08 240 / 45%), transparent);
  }

  .balloon-1 {
    --balloon-w: 2.75rem;
    top: 10%;
    left: 12%;
    animation-delay: 0s;
    animation-duration: 9s;
  }

  .balloon-2 {
    --balloon-w: 2rem;
    top: 18%;
    right: 8%;
    animation-delay: -2s;
    animation-duration: 7.5s;
  }

  .balloon-3 {
    --balloon-w: 3rem;
    bottom: 22%;
    left: 5%;
    animation-delay: -4s;
    animation-duration: 10s;
  }

  .balloon-4 {
    --balloon-w: 2.25rem;
    bottom: 32%;
    right: 6%;
    animation-delay: -1.5s;
    animation-duration: 8.5s;
  }

  .balloon-5 {
    --balloon-w: 1.75rem;
    top: 55%;
    right: 18%;
    animation-delay: -3.5s;
    animation-duration: 7s;
  }

  .balloon-6 {
    --balloon-w: 2.1rem;
    top: 38%;
    left: 3%;
    animation-delay: -5s;
    animation-duration: 9.5s;
  }

  @keyframes balloon-float {
    0%, 100% {
      transform: translateY(0) translateX(0) rotate(-2deg);
    }
    25% {
      transform: translateY(-18px) translateX(8px) rotate(2deg);
    }
    50% {
      transform: translateY(-8px) translateX(-6px) rotate(-1deg);
    }
    75% {
      transform: translateY(-22px) translateX(4px) rotate(1.5deg);
    }
  }

  @keyframes star-float {
    0%, 100% {
      transform: translateY(0) rotate(0deg);
    }
    50% {
      transform: translateY(-12px) rotate(12deg);
    }
  }

  @keyframes star-twinkle {
    0%, 100% {
      opacity: 0.45;
    }
    50% {
      opacity: 1;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .floating-star,
    .floating-balloon {
      animation: none;
    }
  }

  @media (max-width: 640px) {
    .balloon-5,
    .balloon-6,
    .star-7,
    .star-8 {
      display: none;
    }
  }
</style>
